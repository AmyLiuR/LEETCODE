class Solution:
    def isSubsequence(self, s: str, t: str) -> bool:
        i=0
        j=0
        
        for letter in t:
            if i==len(s):
                return True
            if s[i]==letter:
                i+=1
            else:
                pass
        
        return True if i==len(s) else False
